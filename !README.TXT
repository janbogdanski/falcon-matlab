orizontal_plane.m is a simple example showing how to use Haptik from matlab (with polling).
haptikdevice_list is an utility function which returns (and prints) a list of exposed devices.
the directory @haptikdevice contains an utility class to simplify using Haptik from matlab.